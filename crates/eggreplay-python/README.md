# eggreplay Python bindings

The Python package is built from the Rust workspace with maturin. The initial
package exposes only the ABI/toolchain preflight API; product bindings are
added by the M012B–M012F milestones.
