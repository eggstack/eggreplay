# v0.1 Corrective Closure

Status: closed

This record supersedes the support/qualification claims of the original
`v0.1-foundation-closure.md` while retaining it as history. Do not rewrite the
historical artifact.

## Audit baseline and implementation commits

- Audit baseline: `951991e4e4d726a029e703a406d31e4350230153` (M008 close, original v0.1 claims; post-closure audit found gaps listed in `registry.md`).
- Corrective registration: `3562c6f` (`plans: register v0.1 corrective workstream`).
- C001 implementation + closure: `9823b67` (`feat(c001): lazy replay streaming with bounded fixture materialization`).
- C002 implementation + closure: `928f5af` (`feat(c002): concurrent recording session without global lock`).
- C003 implementation + closure: `2738f1e` (`feat(c003): persistence-time redaction before publication`).
- C004 implementation + closure: `bf51d68` (`feat(c004): CLI Eggress routes, exit codes, JSON/JUnit, inspect bodies`).
- Final commit under qualification: the commit containing this record, the C005 status update, and the registry update.

## Verification evidence

Local commands (all passed):

```text
cargo fmt --all -- --check
cargo check --workspace --all-targets --all-features --locked
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --all-features --locked
cargo audit
cargo tree boundary checks (see CI `dependency-boundary` job)
cargo run -p eggreplay-cli -- --version
cargo run -p eggreplay-cli -- --help
```

- `cargo audit`: passed, 236 locked dependencies scanned, no advisories (exit 0).
- `cargo test`: 56 tests (8 core + 21 http unit + 12 http qualification + 8 store + 7 CLI integration), 0 failures.
- Hosted CI: no hosted run available in this local session. CI config (`.github/workflows/ci.yml`) now covers Linux stable + 1.89 MSRV plus stable macOS/Windows, `--locked`, and a `dependency-boundary` job proving core/store transport-freedom, direct builds without Eggress, and narrow `pproxy-compat` only.

## Test counts and scenario inventory (C005 items 1–30)

- 1 GET/no body: replay `lazy_load`, gateway overlap (GET), qual chunked/DNS/timeout (GET).
- 2 POST binary: recording large upload (0xAB 256 KiB), qual chunked.
- 3 repeated headers: `conversion_preserves_repeated_headers`.
- 4 repeated query keys: same test (`a=1&a=2`), qual_03_04.
- 5 multiple Set-Cookie: `qual_05` (two fields preserved, redacted).
- 6 request/response trailers: C001 trailer fixture + stream tests; request trailers via tee/take_trailers.
- 7 HEAD/204 suppression: `qual_07` (HEAD/204 Empty, EggServe normalization delegated).
- 8 unknown-length/streaming: `qual_08` (chunked, no Content-Length).
- 9 large streaming upload/response: C002 gateway large (256 KiB/512 KiB), C001 2 MiB streaming.
- 10 connection refusal: `records_response_body...` (127.0.0.1:1) + `qual_10_11`.
- 11 DNS failure: `qual_10_11` (`nonexistent.invalid.`, stable category, no Internet).
- 12 TLS verification failure: `qual_12` (https:// to plaintext listener, safe Error).
- 13 response-head timeout: `qual_13` (hanging upstream + 300 ms client timeout → Timeout).
- 14 cancellation/mid-body: C002 cancellation abort test.
- 15 repeated consumption/exhaustion: `qual_15` (Once/Exhausted/Unlimited) + strict matching test.
- 16 strict mismatch/near miss: semantic near-miss bounded + strict NoMatch.
- 17 volatile headers: `qual_17` (practical ignores date/user-agent/x-request-id).
- 18 semantic JSON: `qual_18` + semantic tests.
- 19 default credential redaction: C003 `default_sensitive_headers_never_persisted`.
- 20 configured query/JSON redaction + sentinel scan: C003 `configured_query_and_json...` (full-dir scans).
- 21 direct client replay: C001 server test (direct EggFetch) + C004 CLI direct replay.
- 22 Eggress-routed replay: C004 `eggress_http_proxy_route_replays_from_cli` + two-hop unit.
- 23 target remapping: `qual_23` (path/query preserved across authority remap).
- 24 status/header/body/trailer findings: `qual_24`.
- 25 corrupt/symlinked blob rejection: store `open_blob` symlink + C001 corrupt integrity.
- 26 lazy large-response replay: C001 (6 MiB fixture, metadata-only load, 64 KiB chunks).
- 27 overlapping concurrent recording: C002 gateway overlap (second start < first end).
- 28 exit-code contract: C004 CLI subprocess (0 direct validate, 1 regression/diff, 2 malformed/invalid, 3 missing fixture, 4 bind failure path, 5 mapping fallback) + `exit_code_for_class`.
- 29 JSON envelope contract: C004 subprocess stdout JSON + stderr agreement.
- 30 per-flow JUnit: C004 `tests="2"` with stable flow-id cases.

No routine test uses public Internet.

## Platform and protocol matrix

- Rust 1.89 MSRV (Linux) + stable (Linux/macOS/Windows via CI).
- Direct HTTP/1.1 acquisition and EggServe inbound HTTP/1.1 replay (tested/supported). H2/H3 deferred, untested, no support claim.
- Optional listener-free Eggress TCP routing via narrow `pproxy-compat` only.
- Loopback transport qualification runs uniformly; no platform-specific network tests claimed silently.

## Dependency versions and feature set

- EggFetch 0.2.0 (crates.io).
- EggServe primitives/server pinned `859a2bc2fe1d9e6215c122d19cd50f2f070d2da6`.
- Eggress outbound pinned `c493eab803654d1316150a9837fd01ee46b9b87d` (v1.0.7) with only `pproxy-compat` enabled; extended/SSH/QUIC/listener/server never enabled (CI boundary job enforces).
- `cargo tree` proves core/store pull no EggFetch/EggServe/Eggress/Tokio/Hyper; direct HTTP builds without Eggress.

## Resource, streaming, and concurrency evidence

- Lazy replay: 3×2 MiB fixture loads with descriptors only (missing-blob-after-open still loads); selected 2 MiB streams in ≤64 KiB chunks with exact bytes + hash.
- Concurrent recorder: 10×32 KiB chunked writes with sleeps overlap (second start < first end); gateway overlap via upstream timings; 20-thread append preserves count.
- Large-body chunking bounded at 64 KiB (replay) with no whole-payload clones (concurrent streams test).
- Near-miss bounded by `max_near_misses` (semantic test asserts 1) and loader called only for narrowed candidates.

Wall-clock numbers are evidence only, not CI gates.

## Security and redaction evidence

- `cargo audit` passed (236 deps, 0 advisories).
- Sentinel scans: C003 full-fixture directory scans for header/query/request-JSON/response-JSON sentinels (absent from blobs, flow JSON, markers, error/log captures). Secret `SENTINEL` strings unique per test.
- Default headers (Authorization, Proxy-Authorization, Cookie, Set-Cookie) never persist; userinfo never in authority/diagnostics; malformed/oversized structured bodies fail closed; framing reconciled (Content-Length recomputed, MD5/Digest/Signature + strong ETags removed, weak preserved with warning); matching wildcards redacted fields; `inspect` exposes profile/markers without values; opaque binary discovery explicitly unsupported.

## Remaining limitations

- H2/H3, WebSocket, TLS interception, authored scenarios/templates, timing/SSE profiles, Python bindings, HAR import/export remain deferred (M009–M014, still deferred, not unblocked by this closure as they are future roadmap, not v0.1 dependencies).
- Opaque binary bodies cannot be selectively redacted; large structured bodies beyond 1 MiB with requested redaction fail closed by design.
- `replay` reports differences with exit 0; `test`/`diff` enforce with exit 1. Internal exit 5 has no deterministic subprocess trigger (fallback for bugs, mapping-tested).
- No hosted CI run in this local session; CI config updated for declared matrix with `--locked`.

## Superseding statement

This record supersedes the support/qualification claims of the original
`v0.1-foundation-closure.md` while retaining it as history. The v0.1 corrective
gate is closed only with C001–C004 closed, hosted CI green for the declared
matrix (pending remote run), and no known audit finding mislabeled as supported.
