# M008 — v0.1 Security, Documentation, and Qualification Closure

Status: blocked
Depends on: M001–M007
Release gate: v0.1 closure

## Objective

Close the first release with evidence. Fix defects but do not expand into M009+ scope.

## Security

- default sensitive-header/cookie redaction;
- URL userinfo stripping;
- configured query and structured-body redaction;
- safe diagnostics/Debug/Display;
- fixture permission handling;
- malicious/corrupt fixture bounds;
- path/symlink confinement review;
- dependency/advisory review;
- document residual risk for opaque bodies.

## Qualification

Run every required scenario in `003-qualification-and-release-strategy.md`. Add regressions for failures found.

Explicitly prove bounded large-body memory, incomplete-recording invalidation, deterministic matcher/replay behavior, stable JSON/exit contracts, EggFetch/EggServe/Eggress boundaries, and absence of duplicated network-stack implementations.

## Documentation

Before closure: root quickstart; architecture; `.eggr` schema; matcher profiles; security/redaction guide; CLI/exit codes; regression cookbook; Eggress routing guide; non-goals/limitations; release process.

## Packaging

Prepare standalone CLI releases for supported desktop/server targets consistent with current Eggstack binary-release conventions. Python packaging remains M012.

## Closure record

Create `plans/closure/v0.1-foundation-closure.md` with baseline/final commits, exact test commands/results, CI evidence, supported matrix, schema/dependency versions, streaming/performance evidence, known limitations, and deferred milestones. Then close M001–M008 in the registry.

## Definition of done

A fresh user can record, validate, replay offline, diagnose mismatch, run direct and Eggress-routed regression tests, consume JSON/JUnit, and understand fixture data/security limits.
