# Regression cookbook

Use `replay` to observe a baseline against a target without enforcing
assertions. Use `test` to enforce the same typed report and stable exit class.
Use `diff` to compare two validated fixtures offline. Reports distinguish
status, selected headers/trailers, body digest/length, outcome category/phase,
and timing assertions when configured.
