# Release process

Run formatting, workspace check, clippy, all-feature tests, local qualification,
and dependency/advisory review. Build the standalone CLI for the supported
desktop/server targets with the declared Rust MSRV. Record exact commands,
dependency revisions, supported protocol matrix, known limitations, and
deferred milestones in the v0.1 closure record before tagging.
